#include "../common/pre_compile.h"

size_t login_handler(c2::server::core::session * sess, char * in_buffer)
{
	return size_t();
}

size_t create_room_handler(c2::server::core::session * sess, char * in_buffer)
{
	return size_t();
}

size_t enter_room_handler(c2::server::core::session * sess, char * in_buffer)
{
	return size_t();
}

size_t leave_room_handler(c2::server::core::session * sess, char * in_buffer)
{
	return size_t();
}

size_t whisper_handler(c2::server::core::session * sess, char * in_buffer)
{
	return size_t();
}

size_t chatting_handler(c2::server::core::session * sess, char * in_buffer)
{
	return size_t();
}

size_t select_room_handler(session * sess, char * in_buffer)
{
	return size_t();
}

size_t select_user_in_room_handler(c2::server::core::session * sess, char * in_buffer)
{
	return size_t();
}

size_t select_all_handler(c2::server::core::session * sess, char * in_buffer)
{
	return size_t();
}
